#include "main.h"
/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */
void _puts(char *str);
int main(void)
{
	_puts("\"Programming is like building a multilingual puzzle");
	return (0);
}

